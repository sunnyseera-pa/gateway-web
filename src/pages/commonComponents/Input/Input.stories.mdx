import { Meta, Story, Canvas } from '@storybook/addon-docs';

import Input from './Input';
import Icon from '../Icon';

<Meta title='Input' component={Input} />

<Canvas>
	<Story name='Basic'>
		{props => (
			<Input
				{...props}
				iconPrepend={<Icon name='search' />}
				iconAppend={
					<>
						<Icon name='close-alt' fill='red600' color='red600' size='xs' />
						<Icon name='tick' fill='green600' color='green600' ml={2} inline size='xs' />
					</>
				}
			/>
		)}
	</Story>
</Canvas>
