import { Meta, Story, Canvas } from '@storybook/addon-docs';

import Icon from './Icon';

<Meta
	title='Icon'
	component={Icon}
	args={{
		name: 'tick',
		...Icon.defaultProps,
	}}
	argTypes={{
		size: {
			options: ['x-small', 'small', 'default', 'large', 'x-large', 'xx-large', 'xxx-large'],
			control: { type: 'radio' },
		},
		color: {
			control: 'text',
		},
	}}
/>

<Canvas>
	<Story name='Basic'>{props => <Icon {...props} />}</Story>
</Canvas>
